[PUBLIC]
   path = /opt/public_share
   read only = No
   writable = Yes
   valid users = @mms @sambashare @guest
   force user = mms
   veto files = /.*/
   hide unreadable = yes
   create mask = 0000
   force create mode = 0644
   directory mask = 0000
   force directory mode = 755
   browsable = yes
